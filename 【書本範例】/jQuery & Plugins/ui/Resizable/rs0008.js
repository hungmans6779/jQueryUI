$(document).ready(function(){
$("#rs").resizable({ 
   handles: "all", 
    minWidth: 100, 
    minHeight: 100, 
    maxWidth: 300, 
    maxHeight: 300 
    });
   $("#img").resizable({ 
   handles: "all", 
    minWidth: 100, 
    minHeight: 100, 
    maxWidth: 300, 
    maxHeight: 300  
    });
});